Installation:

    git clone git@github.com:nadilsons/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/dot.vimrc ~/.vimrc
    ln -s ~/.vim/dot.gvimrc ~/.gvimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
